apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-luklak-student-service.fullname" . }}-test-connection"
  labels:
    {{- include "helm-luklak-student-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-luklak-student-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
